sudo: false
language: ruby
rvm:
  - 2.2.8
  - 2.3.5
  - 2.4.2
  - ruby-head
before_install: gem install bundler -v 1.15.4
script:
  - bundle exec rubocop
  - bundle exec rspec
matrix:
  fast_finish: true
  allow_failures:
    - rvm: ruby-head
